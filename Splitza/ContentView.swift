//
//  ContentView.swift
//  Splitza
//
//  Legacy file — no longer used. App entry is via RootBuilder -> RootView.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Use RootView via RootBuilder")
    }
}
